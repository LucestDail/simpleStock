이 디렉터리의 `*.json` 파일은 앱이 실행 중 생성하고 갱신하는 런타임 데이터입니다.

- `portfolio.json`: 보유 자산/스냅샷
- `chat.json`: 대화 스레드/메시지
- `memory.json`: 장기 기억/브리프/예약/시장 캐시
- `profile.json`: 사용자 프로필/AI 추론 요약

이 파일들은 Git에서 추적하지 않습니다.
배포 환경에서는 바인드 마운트된 실제 데이터가 유지되고, 파일이 없으면 서버가 기본값으로 다시 생성합니다.
