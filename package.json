{
  "name": "simple-stock",
  "version": "2.0.0",
  "private": true,
  "description": "개인 전용 자산 현황·일 단위 변동 추적",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm ci && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.0.0",
    "@langchain/langgraph": "^1.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "node-cron": "^4.1.0",
    "zod": "^4.4.3"
  }
}
